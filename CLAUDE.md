# Git commits

- Do not add a `Co-Authored-By: Claude` (or similar AI attribution) trailer to commits in this repo. This repo doubles as a public portfolio piece meant to showcase Riyaz's own engineering judgment — the README already discloses the Claude Code collaboration honestly, so commit-level trailers on every change would just amplify it in a way that undersells the review/decision-making he's actually doing.
